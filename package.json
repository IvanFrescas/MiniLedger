{
  "name": "miniledger",
  "version": "1.0.0",
  "description": "A simple implementation of the ledger cli",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ivan Frescas",
  "license": "ISC",
  "dependencies": {
    "commander": "^3.0.2"
  }
}
